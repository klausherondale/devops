# GradeTrack — Simple Multi-page Dashboard

This folder contains a ready-to-deploy static site (HTML/CSS/JS) for GitHub Pages.

Open `index.html` to view the Dashboard.

Deploy: push this repo to GitHub and enable Pages from `main` branch (root).
